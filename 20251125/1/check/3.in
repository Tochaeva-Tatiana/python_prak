class TestClass(metaclass=dump):
        def __init__(self, x):
            self.x = x
        
        def add(self, y):
            return self.x + y
    
obj = TestClass(5)
result = obj.add(3)
print(result)